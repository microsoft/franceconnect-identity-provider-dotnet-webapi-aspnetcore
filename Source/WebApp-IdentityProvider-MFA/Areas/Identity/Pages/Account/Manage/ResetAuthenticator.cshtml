@page
@model ResetAuthenticatorModel
@{
    ViewData["Title"] = "Oublier les applications d'authentification";
    ViewData["ActivePage"] = ManageNavPages.TwoFactorAuthentication;
}

<partial name="_StatusMessage" for="StatusMessage" />
<h3>@ViewData["Title"]</h3>
<div class="alert alert-warning" role="alert">
    <p>
        <span class="glyphicon glyphicon-warning-sign"></span>
        <strong>Si vous oubliez vos applications d'authentification, vous ne pourrez plus les utiliser pour vous connecter à ce compte jusqu'à ce que vous les ayiez réenregistrées.</strong>
    </p>
    <p>
        Ce processus peut désactiver l'authentification à deux facteurs jusqu'à ce qu'elle soit reconfigurée.
    </p>
</div>
<div>
    <form id="reset-authenticator-form" method="post">
        <button id="reset-authenticator-button" class="btn btn-danger" type="submit">Oublier vos applications d'authentification</button>
    </form>
</div>
